FROM ruby:2.6-alpine

ARG SERVER
ARG LIBRARY

WORKDIR /srv/app

COPY ${LIBRARY} /srv/app

RUN apk -Uuv add \
    gcc \
    make \
    libc-dev

RUN gem install bundle ${SERVER}

RUN ls -l /srv/app

RUN bundle install
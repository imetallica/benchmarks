FROM jruby:9.0

ARG SERVER
ARG LIBRARY

WORKDIR /srv/app

COPY apps/${LIBRARY} /srv/app

RUN apk -Uuv add \
    gcc \
    make \
    libc-dev

RUN gem install bundle ${SERVER}

RUN ls -l /srv/app

RUN bundle install
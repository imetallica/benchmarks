FROM jruby:9.0

ARG SERVER
ARG LIBRARY

WORKDIR /srv/app

COPY apps/${LIBRARY} /srv/app

RUN apt-get install -yq \
    libc6-dev \
    build-essential

RUN gem install bundle ${SERVER}

RUN ls -l /srv/app

RUN bundle install